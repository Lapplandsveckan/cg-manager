{
  "private": true,
  "name": "cg-manager",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "yarn workspace @lappis/cg-manager build",
    "package": "yarn workspace @lappis/cg-manager build && yarn workspace @cg-manager/server package",
    "gen:web-lib-types": "yarn workspace @cg-manager/server gen:web-lib-types",
    "start": "yarn workspace @lappis/cg-manager build && yarn workspace @cg-manager/server start",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces run lint:fix",
    "format": "yarn workspaces run format",
    "format:check": "yarn workspaces run format:check",
    "tsc": "yarn workspaces run tsc"
  },
  "resolutions": {
    "strip-ansi": "^6.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import-x": "^4",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.11.0",
    "prettier": "^3.8.3",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8"
  }
}
